# DMS Frontend (Angular)

Minimal Angular app with:

- Document list (search)
- Upload form (metadata + first chunk demo)
- Detail view (download demo)

## Setup

```bash
cd frontend/dms-frontend
npm install
npm start
```

Make sure backend gateway is running at `http://localhost:8080`.
